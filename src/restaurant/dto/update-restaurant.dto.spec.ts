import { UpdateRestaurantDto } from './update-restaurant.dto';

describe('UpdateRestaurantDto', () => {
  it('should be defined', () => {
    expect(new UpdateRestaurantDto()).toBeDefined();
  });
});
